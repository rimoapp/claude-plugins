{
  "name": "auto-worktree",
  "version": "1.0.0",
  "description": "Automatically creates git worktrees when Claude modifies files, enabling safe parallel work without git conflicts.",
  "author": {
    "name": "Rimo",
    "email": "support@rimo.app",
    "url": "https://rimo.app"
  },
  "repository": "https://github.com/rimo/claude-plugins",
  "license": "MIT",
  "userConfig": {
    "skip_directories": {
      "title": "Skip Directories",
      "type": "string",
      "description": "Comma-separated list of git repository root paths where auto-worktree should not activate (e.g. /path/to/notes,/path/to/scratch)",
      "sensitive": false
    },
    "pull_default_branch": {
      "title": "Pull Default Branch",
      "type": "boolean",
      "description": "Pull the latest default branch from origin on session start. Uses fast-forward only, so local changes are never overwritten. If the pull fails (offline, timeout, or diverged history), the plugin silently continues with the local state. (default: true)",
      "sensitive": false
    },
    "sync_gitignored_writes": {
      "title": "Sync Gitignored Writes",
      "type": "boolean",
      "description": "Automatically copy gitignored files written in a worktree back to the main repository. Covers Write/Edit tool calls and Bash output redirects to gitignored paths. (default: true)",
      "sensitive": false
    },
    "auto_return_to_default": {
      "title": "Auto Return to Default Branch",
      "type": "boolean",
      "description": "Automatically switch back to the default branch at session start if on a non-default branch with no uncommitted changes. When disabled, the working branch is left alone; the local default branch ref is still kept fresh by pull_default_branch. (default: true)",
      "sensitive": false
    }
  }
}
